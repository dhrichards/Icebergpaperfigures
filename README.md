# Icebergpaperfigures
Code to reproduce figures in "A viscoelastic phase-field model for calving and fracture in ice

The python files in `scripts` can be run and checkpoints saved, which can then be loaded by the files in `figures`